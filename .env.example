MONGODB_URI=mongodb+srv://contact_db_user:VM7vPjtxhkZvOlnU@clusteradhiuman.znzeltu.mongodb.net/Adhiuman?appName=ClusterAdhiuman
PORT=5002
JWT_SECRET=adihuman-secret-key-change-in-production
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Product Extraction (superadmin catalog PDF -> product pipeline)
# Requires poppler-utils (pdftoppm, pdfimages) on PATH: `brew install poppler` locally,
# `apk add poppler-utils` in the server Docker image. Extraction is OCR + layout heuristics
# (tesseract.js, already a dependency) - no API key needed.
